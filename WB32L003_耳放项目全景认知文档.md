# WB32L003 耳放项目全景认知文档

> 📚 本文档专为移动端开发背景的读者设计，从零开始构建单片机项目的完整认知体系
> 
> 作者：Claude | 版本：v1.0 | 日期：2025-07-02

---

## 目录

1. [第一章：最小认知框架 - 10分钟快速入门](#第一章最小认知框架)
2. [第二章：系统架构 - 项目全景图](#第二章系统架构)
3. [第三章：核心概念 - 单片机的五大系统](#第三章核心概念)
4. [第四章：项目实现 - 代码如何工作](#第四章项目实现)
5. [第五章：实战操作 - 烧录与测试](#第五章实战操作)
6. [第六章：沟通指南 - 与硬件工程师协作](#第六章沟通指南)

---

## 第一章：最小认知框架

### 1.1 什么是单片机？

🎯 **一句话理解**：单片机就是一个"微型电脑"，可以接收信号、处理信息、控制设备。

🌍 **生活类比**：
想象一个智能遥控器：
- 你按下按钮（输入）
- 遥控器识别按钮（处理）
- 发出控制信号（输出）
- 电视执行命令（结果）

单片机就是这样的"智能控制中心"，只是它更加灵活和强大。

```plantuml
@startuml
skinparam defaultTextAlignment center
skinparam rectangle {
    BackgroundColor #E6F3FF
    BorderColor #4A90E2
    FontColor #2C3E50
}

rectangle "输入设备\n• 按键\n• 传感器\n• 通信接口" as input #FFE6E6

rectangle "单片机\n• 接收信号\n• 处理逻辑\n• 发出指令" as mcu #E6FFE6

rectangle "输出设备\n• 显示屏\n• LED灯\n• 蜂鸣器" as output #E6E6FF

input --> mcu : 信号
mcu --> output : 控制
@enduml
```

### 1.2 我们的项目在做什么？

🎯 **一句话理解**：做一个"音乐可视化器"，把声音的大小用灯光条显示出来。

🌍 **生活类比**：
就像汽车音响上的音量指示灯：
- 音乐响，灯就多亮几格
- 音乐轻，灯就少亮几格
- 左右声道分别显示

```plantuml
@startuml
skinparam defaultTextAlignment center
skinparam componentStyle rectangle

package "音频输入层" {
    component "左声道 (L)" as left_audio #FFB3B3
    component "右声道 (R)" as right_audio #FFB3B3
    component "电源音源" as source #FFCCCC
}

package "信号调理层" {
    component "分压电路\n0~3.3V" as divider #FFF3B3
    component "滤波电路\n抗干扰" as filter #FFF3B3
}

package "WB32L003单片机核心" {
    component "ADC模块\n12位 1kHz" as adc #E6FFE6
    component "Cortex-M0+\n24MHz CPU" as cpu #B3FFB3
    component "GPIO控制器" as gpio #B3FFB3
    component "SPI控制器\n12MHz" as spi #B3FFB3
    component "PWM控制器\nTIM2" as pwm #B3FFB3
}

package "显示驱动层" {
    component "ST7735驱动\n160x80像素" as display_driver #E6B3FF
    component "背光控制\nPWM调光" as backlight #E6B3FF
}

package "用户接口层" {
    component "0.96寸TFT屏\nVU表显示" as screen #B3E6FF
    component "电源按键\n开关机" as power_key #FFFFB3
    component "模式按键\nSTEREO/MONO" as mode_key #FFFFB3
    component "状态LED\n工作指示" as led #FFFFB3
}

package "电源管理层" {
    component "3.7V锂电池" as battery #FFE6B3
    component "电压监测\n低电提醒" as voltage_monitor #FFE6B3
}

' 信号流向
source --> left_audio
source --> right_audio

left_audio --> divider
right_audio --> divider
divider --> filter
filter --> adc

adc --> cpu : PC1/PC2\n数字信号
cpu --> spi : 显示数据
cpu --> gpio : 按键扫描\nLED控制
cpu --> pwm : 背光调节

spi --> display_driver : SPI总线\nPC5/PC6
pwm --> backlight : PA1输出
display_driver --> screen
backlight --> screen

power_key --> gpio : PC13
mode_key --> gpio : PC4
gpio --> led : PB5

battery --> voltage_monitor
voltage_monitor --> adc : PC0\n电压检测
voltage_monitor --> cpu : 电源控制

note bottom of cpu
处理流程：
1. ADC采集 → 音量计算
2. 映射到32级显示
3. SPI刷新VU表
4. 20Hz实时更新
end note

@enduml
```

### 1.3 为什么需要单片机？

💡 **关键理解**：
- **实时性**：每秒采集1000次音频，人眼看到流畅动画
- **智能化**：自动调节背光、监测电池、切换模式
- **低功耗**：不工作时进入睡眠，延长电池寿命
- **可编程**：通过代码实现各种功能，灵活升级

---

## 第二章：系统架构

### 2.1 硬件架构全景

#### 系统概念图

```plantuml
@startuml
skinparam defaultTextAlignment center
skinparam rectangle {
    BackgroundColor #F0F0F0
    BorderColor #333333
}

rectangle "WB32L003 MCU\nCPU: Cortex-M0+\nFlash: 64KB\nRAM: 4KB\n时钟: 24MHz" as mcu #FFE6B3

rectangle "电源系统\n• 3.7V锂电池\n• 电压监测\n• 充电检测" as power #B3E6FF

rectangle "音频输入\n• 左声道 (L_AD)\n• 右声道 (R_AD)\n• ADC采集" as audio #FFB3B3

rectangle "显示系统\n• 0.96\" TFT屏\n• ST7735驱动\n• 160x80像素" as display #B3FFB3

rectangle "用户接口\n• 电源键\n• 模式键\n• 红色LED" as ui #FFB3FF

rectangle "调试接口\n• SWD烧录\n• UART调试" as debug #FFFFB3

power --> mcu : 3.3V供电
audio --> mcu : ADC\n(PC1/PC2)
mcu --> display : SPI\n(PC5/PC6)
ui --> mcu : GPIO\n(PC13/PC4)
debug <--> mcu : 调试
@enduml
```

### 2.2 硬件连接概览

🎯 **理解重点**：各模块如何协同工作

💡 **工程思维**：
- **模块化设计**：每个部分都有明确职责
- **接口标准化**：SPI、GPIO等通用接口
- **信号流向清晰**：输入→处理→输出

### 2.3 系统信号流

```plantuml
@startuml
skinparam defaultTextAlignment center
skinparam rectangle {
    BackgroundColor #F0F0F0
    BorderColor #333333
}

rectangle "WB32L003 MCU\nCPU: Cortex-M0+\nFlash: 64KB\nRAM: 4KB\n时钟: 24MHz" as mcu #FFE6B3

rectangle "电源系统\n• 3.7V锂电池\n• 电压监测\n• 充电检测" as power #B3E6FF

rectangle "音频输入\n• 左声道 (L_AD)\n• 右声道 (R_AD)\n• ADC采集" as audio #FFB3B3

rectangle "显示系统\n• 0.96\" TFT屏\n• ST7735驱动\n• 160x80像素" as display #B3FFB3

rectangle "用户接口\n• 电源键\n• 模式键\n• 红色LED" as ui #FFB3FF

rectangle "调试接口\n• SWD烧录\n• UART调试" as debug #FFFFB3

power --> mcu : 3.3V供电
audio --> mcu : ADC\n(PC1/PC2)
mcu --> display : SPI\n(PC5/PC6)
ui --> mcu : GPIO\n(PC13/PC4)
debug <--> mcu : 调试
@enduml
```

### 2.4 软件架构分层

```plantuml
@startuml
skinparam defaultTextAlignment center

package "应用层" #E6F3FF {
    [主循环 main.c]
    [用户界面 ui.c]
}

package "功能层" #F0E6FF {
    [音频处理 audio_adc.c]
    [电源管理 power.c]
    [硬件控制 hardware_control.c]
}

package "驱动层" #FFE6F0 {
    [显示驱动 st7735.c]
    [HAL库驱动]
}

package "硬件层" #E6FFE6 {
    [GPIO引脚]
    [SPI接口]
    [ADC模块]
    [定时器]
}

[主循环 main.c] --> [用户界面 ui.c]
[主循环 main.c] --> [音频处理 audio_adc.c]
[主循环 main.c] --> [电源管理 power.c]

[用户界面 ui.c] --> [显示驱动 st7735.c]
[音频处理 audio_adc.c] --> [硬件控制 hardware_control.c]
[电源管理 power.c] --> [硬件控制 hardware_control.c]

[显示驱动 st7735.c] --> [HAL库驱动]
[硬件控制 hardware_control.c] --> [HAL库驱动]

[HAL库驱动] --> [GPIO引脚]
[HAL库驱动] --> [SPI接口]
[HAL库驱动] --> [ADC模块]
[HAL库驱动] --> [定时器]
@enduml
```

### 2.5 数据流向图

```plantuml
@startuml
skinparam defaultTextAlignment center
skinparam activity {
    BackgroundColor #E6F3FF
    BorderColor #4A90E2
    FontColor #2C3E50
}

start

:音频信号输入;
note right: 左右声道模拟信号

:ADC采样;
note right: 1kHz采样率\n12位精度

:数字信号处理;
note right: 计算音量等级\n0-31级

:更新显示缓存;
note right: 准备VU表数据

:SPI传输;
note right: 12MHz高速传输

:TFT显示;
note right: 刷新VU表动画

stop
@enduml
```

---

## 第三章：核心概念

### 3.1 GPIO - 单片机的手脚

🎯 **一句话理解**：GPIO就是单片机的"开关"，可以控制高低电平，也可以读取外部信号。

🌍 **生活类比**：
- **输出模式**：像家里的开关，可以控制灯的亮灭
- **输入模式**：像门铃按钮，可以检测是否被按下

```plantuml
@startuml
skinparam defaultTextAlignment center

rectangle "GPIO引脚\n可以设置为:\n• 输入模式\n• 输出模式\n• 复用功能" as gpio #FFE6B3

rectangle "输出示例\nGPIO_PIN_SET (高电平)\n= LED灭\n——————\nGPIO_PIN_RESET (低电平)\n= LED亮" as output #B3E6FF

rectangle "输入示例\n读取按键状态\n——————\n按下 = 低电平\n松开 = 高电平" as input #B3FFB3

gpio --> output : 作为输出
gpio --> input : 作为输入
@enduml
```

💡 **项目中的应用**：
```c
// 点亮红色LED（低电平有效）
HAL_GPIO_WritePin(LED_RED_PORT, LED_RED_PIN, GPIO_PIN_RESET);

// 读取电源按键状态
if (HAL_GPIO_ReadPin(KEY_PWR_PORT, KEY_PWR_PIN) == GPIO_PIN_RESET) {
    // 按键被按下
}
```

### 3.2 ADC - 模拟世界的翻译官

🎯 **一句话理解**：ADC把连续的模拟信号（如声音）转换成数字（如0-4095）。

🌍 **生活类比**：
像温度计的工作原理：
- 温度是连续变化的（模拟）
- 温度计显示具体数字（数字）
- 精度决定了能分辨多细微的变化

```plantuml
@startuml
skinparam defaultTextAlignment center

rectangle "模拟信号\n• 音频波形\n• 0-3.3V连续变化" as analog #FFE6E6

rectangle "ADC转换\n• 12位精度\n• 0-4095数值\n• 1kHz采样" as adc #E6FFE6

rectangle "数字处理\n• 计算平均值\n• 检测峰值\n• 转换为0-31级" as digital #E6E6FF

analog --> adc : 采样
adc --> digital : 数字值
@enduml
```

💡 **关键参数理解**：
- **12位精度**：能分辨 3.3V/4096 = 0.8mV 的变化
- **1kHz采样率**：每秒采集1000次，捕捉音频变化
- **3通道扫描**：同时采集电池、左声道、右声道

### 3.3 SPI - 高速数据传输

🎯 **一句话理解**：SPI是一种"传送带"，快速把数据从单片机送到显示屏。

🌍 **生活类比**：
像工厂的流水线：
- **SCK（时钟）**：传送带的速度
- **MOSI（数据）**：传送带上的货物
- **CS（片选）**：选择哪条流水线工作

```plantuml
@startuml
skinparam sequence {
    ArrowColor #4A90E2
    ActorBorderColor #4A90E2
    LifeLineBorderColor #4A90E2
    ParticipantBorderColor #4A90E2
}

participant "单片机" as mcu
participant "显示屏" as lcd

note over mcu,lcd: SPI通信时序

mcu -> lcd: CS拉低（选中设备）
mcu -> lcd: 发送命令字节
note right: SCK提供时钟\nMOSI传输数据
mcu -> lcd: 发送数据字节
mcu -> lcd: 发送数据字节
mcu -> lcd: CS拉高（释放设备）

note over lcd: 显示屏更新画面

@enduml
```

💡 **为什么用SPI？**
- **速度快**：12MHz，每秒传输150万字节
- **实时性**：保证动画流畅不卡顿
- **简单可靠**：只需要4根线

### 3.4 PWM - 亮度调节器

🎯 **一句话理解**：PWM通过快速开关来调节平均功率，实现亮度调节。

🌍 **生活类比**：
像电风扇的调速：
- 一直开 = 最大风速
- 开一会关一会 = 中等风速
- 开的时间很短 = 微风

```plantuml
@startuml
skinparam defaultTextAlignment center

rectangle "PWM原理\n占空比 = 高电平时间 / 周期" as pwm #FFE6B3

rectangle "25%占空比\n▅▁▁▁▅▁▁▁\n背光较暗" as d25 #E6F3FF

rectangle "50%占空比\n▅▅▁▁▅▅▁▁\n背光适中" as d50 #E6F3FF

rectangle "75%占空比\n▅▅▅▁▅▅▅▁\n背光较亮" as d75 #E6F3FF

pwm --> d25
pwm --> d50
pwm --> d75
@enduml
```

### 3.5 中断 - 紧急事件处理

🎯 **一句话理解**：中断就是"紧急电话"，可以立即打断正在做的事情。

🌍 **生活类比**：
你正在看书（主程序），突然：
- 电话响了（中断发生）
- 你放下书去接电话（执行中断程序）
- 接完电话继续看书（返回主程序）

---

## 第四章：项目实现

### 4.1 物理架构深入

#### 硬件分层结构

当你对系统有了基本理解后，我们来看看实际的物理架构：

```plantuml
@startuml
skinparam defaultTextAlignment center
skinparam componentStyle rectangle

package "PCB主板 (FR4双面板)" {
    component "WB32L003芯片\n32引脚LQFP封装\n5x5mm" as mcu_chip #E6FFE6
    
    component "晶振电路\n24MHz主频" as crystal #F0F0F0
    
    component "电源管理\n• LDO稳压器\n• 去耦电容\n• 滤波电路" as power_circuit #FFE6B3
    
    component "音频输入接口\n• 3.5mm插座\n• 分压电阻\n• 保护电路" as audio_input #FFB3B3
    
    component "按键电路\n• 轻触开关\n• 上拉电阻\n• 防抖电容" as buttons #FFFFB3
    
    component "LED指示\n• 限流电阻\n• 状态指示" as led_circuit #FFB3FF
    
    component "连接器\n• TFT屏接口\n• 调试接口\n• 电源接口" as connectors #E6E6FF
}

package "TFT显示模块 (独立PCB)" {
    component "ST7735控制器\n128x160驱动IC" as display_controller #E6B3FF
    
    component "0.96寸彩色屏\n160x80分辨率\nIPS广视角" as lcd_panel #B3E6FF
    
    component "背光LED\n白光LED阵列" as backlight_led #E6F3FF
    
    component "FPC排线接口\n8Pin连接器" as fpc_connector #F0F0F0
}

package "外壳结构" {
    component "前面板\n透明亚克力" as front_panel #F5F5F5
    component "后壳体\nABS塑料" as back_case #F5F5F5
}

' 物理连接关系
mcu_chip <--> connectors : PCB走线
connectors <--> fpc_connector : FPC排线\n长度: 50mm
fpc_connector <--> display_controller : 内部连线
display_controller --> lcd_panel : 驱动信号
display_controller --> backlight_led : PWM控制

crystal --> mcu_chip : 时钟信号
power_circuit --> mcu_chip : 3.3V供电
audio_input --> mcu_chip : 模拟信号\nPC1/PC2
buttons --> mcu_chip : 数字信号\nPC4/PC13
mcu_chip --> led_circuit : GPIO控制\nPB5

lcd_panel --> front_panel : 可视窗口
connectors --> back_case : 安装固定

note right of fpc_connector
  FPC排线定义：
  1. VCC (3.3V)
  2. GND
  3. SCK (PC5)
  4. SDA (PC6) 
  5. RES (PC3)
  6. DC (PA3)
  7. CS (PB4)
  8. BLK (PA1)
end note

note bottom of mcu_chip
  WB32L003引脚分配：
  • GPIO: 20个可用IO
  • ADC: 3通道 (PC0/1/2)
  • SPI: 1路 (PC5/6)
  • PWM: 4路定时器
  • 工作电压: 2.0-3.6V
  • 功耗: 5mA@24MHz
end note

@enduml
```

#### PCB与组件连接关系

理解了分层结构后，我们来看看实际的连接方式：

```plantuml
@startuml
skinparam defaultTextAlignment center

rectangle "PCB主板布局" as mainboard #F0F8FF {
    rectangle "WB32L003\n(5x5mm)" as mcu #98FB98
    rectangle "TFT连接器\n8Pin 1.25mm间距" as tft_conn #DDA0DD
    rectangle "音频输入\n3.5mm立体声" as audio_jack #FFB6C1
    rectangle "按键组\nPWR + MODE" as buttons #FFFACD
    rectangle "状态LED\n红色指示" as status_led #FF6347
    rectangle "调试接口\nSWD 4Pin" as debug_port #B0E0E6
}

rectangle "TFT模块" as tft_module #E6E6FA {
    rectangle "ST7735S\n驱动IC" as driver_ic #9370DB
    rectangle "液晶屏\n160x80像素" as lcd #8A2BE2
    rectangle "背光LED\n4个白光LED" as backlight #DA70D6
    rectangle "8Pin接口" as tft_port #DDA0DD
}

' 连接线
mcu --> tft_conn : PCB走线
tft_conn --> tft_port : FPC排线\n50mm长度
tft_port --> driver_ic : 板内连线
driver_ic --> lcd : 驱动信号
driver_ic --> backlight : PWM控制

audio_jack --> mcu : PC1/PC2\n音频信号
buttons --> mcu : PC4/PC13\n按键扫描  
mcu --> status_led : PB5\nLED控制
debug_port --> mcu : PC7/PD1\nSWD调试

note top of tft_conn
  FPC线序：
  1-VCC  2-GND
  3-SCK  4-SDA
  5-RES  6-DC
  7-CS   8-BLK
end note

note bottom of mcu
  Pin分配：
  PC5→SCK
  PC6→MOSI  
  PB4→CS
  PA3→DC
  PC3→RST
  PA1→BL
end note

@enduml
```

### 4.2 引脚分配详解

```plantuml
@startuml
skinparam defaultTextAlignment center
skinparam rectangle {
    BackgroundColor #F5F5F5
    BorderColor #333333
}

rectangle "WB32L003\n单片机\n• 32个引脚\n• 3个GPIO端口\n• 多种复用功能" as mcu #FFE6B3

rectangle "显示接口\nPC5: SPI_SCK (时钟)\nPC6: SPI_MOSI (数据)\nPB4: TFT_CS (片选)\nPA3: TFT_DC (命令/数据)\nPC3: TFT_RST (复位)\nPA1: 背光PWM" as display #B3E6FF

rectangle "音频输入\nPC0: 电池电压检测\nPC1: 左声道音频\nPC2: 右声道音频" as audio #FFB3B3

rectangle "用户接口\nPC13: 电源按键\nPC4: 模式按键\nPB5: 红色LED\nPB2: 模式输出" as ui #B3FFB3

rectangle "调试接口\nPC7: SWDIO\nPD1: SWDCLK\nPB6: UART_TX\nPB7: UART_RX" as debug #FFFFB3

mcu --> display : SPI通信
mcu --> audio : ADC采集
mcu --> ui : GPIO控制
mcu --> debug : 调试烧录
@enduml
```

### 图解说明

1. **WB32L003单片机**：
   - **重要性**：这是整个系统的核心，负责处理所有输入和输出信号。
   - **原理**：通过其32个引脚和多种复用功能，可以灵活地连接外设并实现复杂的功能。

2. **显示接口**：
   - **PC5 (SPI_SCK)**：提供时钟信号，确保数据传输同步。
   - **PC6 (SPI_MOSI)**：传输显示数据。
   - **PB4 (TFT_CS)**：片选信号，用于选择显示屏。
   - **PA3 (TFT_DC)**：区分命令和数据。
   - **PC3 (TFT_RST)**：复位显示屏，确保初始化正常。
   - **PA1 (背光PWM)**：通过PWM调节屏幕亮度。
   - **重要性**：这些引脚共同实现显示屏的控制和数据更新。

3. **音频输入**：
   - **PC0 (电池电压检测)**：监测电池电压，提供低电量提醒。
   - **PC1 (左声道音频)** 和 **PC2 (右声道音频)**：采集音频信号，用于音量显示。
   - **重要性**：音频输入是项目的核心功能，直接影响显示效果。

4. **用户接口**：
   - **PC13 (电源按键)**：控制系统开关机。
   - **PC4 (模式按键)**：切换显示模式（如立体声/单声道）。
   - **PB5 (红色LED)**：指示系统状态。
   - **PB2 (模式输出)**：输出当前模式状态。
   - **重要性**：用户接口提供交互功能，提升用户体验。

5. **调试接口**：
   - **PC7 (SWDIO)** 和 **PD1 (SWDCLK)**：用于调试和烧录程序。
   - **PB6 (UART_TX)** 和 **PB7 (UART_RX)**：用于串口通信，输出调试信息。
   - **重要性**：调试接口是开发和测试阶段的关键，确保系统功能正常。

### 4.2 VU表显示原理

```plantuml
@startuml
skinparam activity {
    BackgroundColor #E6F3FF
    BorderColor #4A90E2
}

start

:采集音频信号;
note right: 左右声道ADC值\n范围: 0-4095

:计算音量等级;
note left
  1. 找出最大最小值
  2. 计算峰峰值
  3. 映射到0-31级
end note

:查找显示位置;
note right: 预定义的32个位置\n对应不同的音量

:绘制VU表;
note left
  - 清除上一帧
  - 绘制新位置
  - 更新背景
end note

:等待下一帧;
note right: 20Hz刷新率\n50ms一帧

stop
@enduml
```

### 4.3 音频采集实现

```c
// 音频处理的核心逻辑（简化版）
void Audio_Process() {
    // 1. ADC采集原始数据
    uint16_t left_raw = ADC_GetValue(LEFT_CHANNEL);   // 0-4095
    uint16_t right_raw = ADC_GetValue(RIGHT_CHANNEL); // 0-4095
    
    // 2. 去除直流偏置（音频信号围绕2048变化）
    int16_t left_ac = left_raw - 2048;
    int16_t right_ac = right_raw - 2048;
    
    // 3. 计算音量（取绝对值）
    uint16_t left_level = abs(left_ac);
    uint16_t right_level = abs(right_ac);
    
    // 4. 映射到显示等级（0-31）
    uint8_t left_display = (left_level * 31) / 2048;
    uint8_t right_display = (right_level * 31) / 2048;
    
    // 5. 更新显示
    UI_UpdateVUMeter(left_display, right_display);
}
```

### 4.4 电源管理策略

```plantuml
@startuml
skinparam defaultTextAlignment center

state "正常运行" as normal #B3FFB3
state "背光降低" as dimmed #FFFFB3
state "深度睡眠" as sleep #FFB3B3
state "低电警告" as warning #FFE6B3
state "自动关机" as shutdown #E6E6E6

normal : 屏幕亮
normal : 24MHz运行
normal : ~5mA功耗
dimmed : 20秒无操作
dimmed : 降低背光
dimmed : ~3mA功耗

sleep : 10分钟无操作
sleep : 关闭外设
sleep : ~10μA功耗

warning : 电压<3.0V
warning : 红灯闪烁

shutdown : 电压<2.8V
shutdown : 保存状态
shutdown : 进入待机

[*] --> normal : 开机
normal --> dimmed : 20秒无操作
dimmed --> normal : 任意操作
dimmed --> sleep : 10分钟
sleep --> normal : 按键唤醒

normal --> warning : 电压<3.0V
warning --> shutdown : 电压<2.8V
shutdown --> [*]
@enduml
```

### 4.5 模式切换逻辑

```plantuml
@startuml
skinparam state {
    BackgroundColor #E6F3FF
    BorderColor #4A90E2
}

state "STEREO模式" as stereo
state "MONO模式" as mono

stereo : 立体声模式
stereo : 左右独立显示
stereo : V2输出低电平

mono : 单声道模式
mono : 左右合并显示
mono : V2输出高电平

[*] --> stereo : 开机默认
stereo --> mono : 按MODE键
mono --> stereo : 按MODE键

note right of stereo : 显示两个独立VU表
note right of mono : 显示一个合并VU表

@enduml
```

---

## 第五章：实战操作

### 5.1 固件烧录原理

🎯 **一句话理解**：烧录就是把程序"装进"单片机，像给手机装APP。

🌍 **生活类比**：
- 单片机 = 新手机
- 固件 = 操作系统+APP
- 烧录器 = 数据线
- 烧录 = 安装过程

```plantuml
@startuml
skinparam defaultTextAlignment center

rectangle "电脑\n• 固件文件\n• (.bin/.hex)\n• 烧录软件" as pc #FFE6B3

rectangle "ST-Link\n烧录器\n• USB转SWD\n• 调试功能" as stlink #B3E6FF

rectangle "目标板\n• WB32L003\n• Flash存储器" as board #B3FFB3

pc --> stlink : USB连接
stlink --> board : SWD接口\n(SWDIO+SWDCLK)

note bottom of board
  烧录过程：
  1. 擦除Flash
  2. 写入新程序
  3. 校验数据
  4. 复位运行
end note
@enduml
```

### 5.2 硬件连接方法

```plantuml
@startuml
skinparam defaultTextAlignment center

rectangle "ST-Link V2\n1. 3.3V (红线)\n2. SWDIO (绿线)\n3. SWDCLK (黄线)\n4. GND (黑线)" as stlink #FFE6B3

rectangle "目标板接口\nVDD → 3.3V\nPC7 → SWDIO\nPD1 → SWDCLK\nGND → GND" as target #B3E6FF

stlink --> target : 4线连接

note bottom
  注意事项：
  * 确保电源电压匹配
  * 连接要牢固
  * 注意防静电
  * 不要带电插拔
end note
@enduml
```

### 5.3 烧录步骤详解

#### Windows环境：

```bash
# 1. 安装ST-Link驱动
# 下载地址：st.com

# 2. 安装烧录工具
# STM32 ST-LINK Utility 或 STM32CubeProgrammer

# 3. 连接硬件
# ST-Link → 目标板

# 4. 烧录命令
st-flash write firmware.bin 0x08000000
```

#### macOS/Linux环境：

```bash
# 1. 安装烧录工具
brew install stlink  # macOS
sudo apt install stlink-tools  # Linux

# 2. 检查连接
st-info --probe

# 3. 烧录固件
st-flash write firmware.bin 0x08000000

# 4. 查看输出
# 2025-07-02T10:00:00 INFO common.c: Flash written and verified! jolly good!
```

### 5.4 烧录后的验证

```plantuml
@startuml
skinparam activity {
    BackgroundColor #E6F3FF
    BorderColor #4A90E2
}

start

:连接电源;

:检查电源LED;
if (LED亮?) then (是)
  :电源正常;
else (否)
  #FFB3B3:检查电源连接;
  stop
endif

:检查显示屏;
if (显示开机画面?) then (是)
  :显示正常;
else (否)
  #FFB3B3:检查SPI连接;
  stop
endif

:按MODE键;
if (切换模式?) then (是)
  :按键正常;
else (否)
  #FFB3B3:检查按键连接;
  stop
endif

:输入音频信号;
if (VU表动?) then (是)
  #B3FFB3:烧录成功！;
else (否)
  #FFB3B3:检查ADC配置;
  stop
endif

stop
@enduml
```

### 5.5 常见问题排查

| 问题现象 | 可能原因 | 解决方法 |
|---------|---------|---------|
| 无法识别设备 | 连接错误 | 检查SWDIO/SWDCLK |
| 烧录失败 | Flash被锁 | 先执行擦除命令 |
| 屏幕不亮 | SPI配置错误 | 检查引脚定义 |
| 按键无反应 | GPIO配置错误 | 检查上拉电阻 |
| VU表不动 | ADC未工作 | 检查时钟配置 |

---

## 第六章：沟通指南

### 6.1 硬件术语速查

| 术语 | 含义 | 沟通示例 |
|------|------|----------|
| 上拉/下拉 | 引脚默认电平 | "按键需要上拉电阻吗？" |
| 高/低有效 | 触发电平 | "LED是低电平点亮吗？" |
| 复用功能 | 引脚多用途 | "PC5可以用作SPI吗？" |
| 去耦电容 | 电源滤波 | "MCU电源需要去耦电容吗？" |
| 分压电阻 | 电压调整 | "电池检测的分压比是多少？" |

### 6.2 关键确认清单

与硬件工程师确认时，使用这个清单：

#### 🔌 电源部分
- **MCU供电电压：3.3V？**
  - 确认单片机的工作电压范围是否与设计一致，避免供电不足或过压损坏。
- **电池电压范围：2.8V-4.2V？**
  - 确认电池的工作范围，确保低电量时系统能正常工作，高电量时不会过充。
- **需要LDO稳压吗？**
  - 确认是否需要低压差稳压器来提供稳定的3.3V电源。
- **去耦电容的位置和容值？**
  - 确认去耦电容的安装位置和容量，确保电源噪声被有效滤除。

#### 📡 通信接口
- **SPI最高频率限制？**
  - 确认显示屏或其他外设是否支持设计的SPI时钟频率，避免信号失真。
- **需要上拉电阻的引脚？**
  - 确认哪些引脚需要上拉电阻以避免悬空状态，确保信号稳定。
- **信号线的长度限制？**
  - 确认PCB设计中信号线的长度是否会影响通信质量。
- **是否需要串联电阻？**
  - 确认是否需要在信号线上串联电阻以减少反射或干扰。

#### 🔊 模拟部分
- **音频输入的电压范围？**
  - 确认音频信号的电压范围是否与ADC的输入范围匹配。
- **需要输入保护吗？**
  - 确认是否需要保护电路（如二极管或电阻）来防止过压或反向电流。
- **ADC参考电压是多少？**
  - 确认ADC的参考电压是否稳定，确保采样精度。
- **是否需要滤波电路？**
  - 确认是否需要滤波电路来减少噪声干扰，提高信号质量。

#### 🎛️ 机械部分
- **按键的机械结构？**
  - 确认按键的类型（轻触开关、拨动开关等）是否符合设计要求。
- **是否有硬件消抖？**
  - 确认是否通过电容或RC电路实现按键消抖，避免误触发。
- **连接器的型号规格？**
  - 确认连接器的型号和规格是否与外设兼容。
- **PCB的层数和厚度？**
  - 确认PCB的设计是否满足机械和电气要求。

### 6.3 沟通模板示例

#### 询问引脚变更：
```
Hi，关于新版PCB的引脚分配：
1. 原设计SPI使用PA5/PA7，新版改为PC5/PC6
2. 请确认PC5/PC6支持SPI功能
3. 时钟频率要求：12MHz
4. 是否需要调整PCB走线？
```
- **备注**：此模板用于确认引脚变更是否影响功能实现，确保硬件设计与软件需求一致。

#### 报告问题：
```
发现问题：红色LED不亮
测试条件：PB5输出低电平
预期结果：LED点亮
实际结果：LED不亮
怀疑原因：
1. LED极性接反？
2. 限流电阻太大？
3. 引脚定义错误？
```
- **备注**：此模板用于报告硬件问题，明确测试条件和预期结果，便于硬件工程师快速定位问题。

#### 项目交接要点：
```
向硬件工程师交接时，提供：
1. 引脚分配表（Excel格式）
2. 电气参数要求
3. 时序要求说明
4. 测试用例清单
5. 已知限制说明
```
- **备注**：此模板用于项目交接，确保软硬件团队之间的信息传递清晰完整。

---

## 附录A：快速参考卡

### 引脚功能一览

| 功能分类 | 引脚 | 说明 |
|---------|------|------|
| **SPI显示** | PC5 | 时钟信号 |
| | PC6 | 数据信号 |
| | PB4 | 片选信号 |
| | PA3 | 数据/命令 |
| | PC3 | 复位信号 |
| **PWM背光** | PA1 | TIM2_CH2 |
| **ADC输入** | PC0 | 电池电压 |
| | PC1 | 左声道 |
| | PC2 | 右声道 |
| **用户按键** | PC13 | 电源键 |
| | PC4 | 模式键 |
| **指示灯** | PB5 | 红色LED |
| **调试** | PC7/PD1 | SWD接口 |

### 关键代码位置

| 功能 | 文件 | 主要函数 |
|------|------|----------|
| 主程序 | main.c | main() |
| 引脚定义 | pin_config.h | - |
| 显示驱动 | st7735.c | ST7735_Init() |
| 音频处理 | audio_adc.c | Audio_ADC_Process() |
| 电源管理 | power.c | Power_Process() |
| 界面更新 | ui.c | UI_UpdateVUMeter() |

### 调试命令速查

```bash
# 查看设备
st-info --probe

# 烧录程序
st-flash write firmware.bin 0x08000000

# 擦除Flash
st-flash erase

# 读取Flash
st-flash read output.bin 0x08000000 0x10000

# 复位设备
st-flash reset
```

---

## 附录B：进阶学习路径

### 🎯 如果你想深入了解单片机：

1. **基础知识**
   - 数字电路基础
   - C语言编程
   - 计算机组成原理

2. **进阶技能**
   - RTOS实时系统
   - 低功耗设计
   - EMC电磁兼容

3. **实践项目**
   - LED矩阵显示
   - 温度监控系统
   - 智能小车

### 📚 推荐资源：

- **书籍**：《STM32库开发实战指南》
- **网站**：ST官方文档中心
- **社区**：电子发烧友论坛
- **工具**：STM32CubeMX配置工具

---

## 结语

恭喜你！读到这里，你已经掌握了：

✅ 单片机的基本概念
✅ 项目的完整架构
✅ 关键技术的原理
✅ 实际操作的方法
✅ 与硬件工程师的沟通技巧

记住：
- **理论结合实践**：动手试试比看百遍文档更有用
- **循序渐进**：不要试图一次理解所有细节
- **保持好奇**：遇到问题是学习的最好机会
- **团队协作**：软硬件配合才能做出好产品

祝你在单片机的世界里探索愉快！🚀

---

*本文档持续更新中，最后更新：2025-07-02*