很高兴继续为您核对更详细的硬件假设列表。由于原理图只有一张大图纸（A/B/C/D 坐标标注），以下引用的“页码”都以区域坐标表示，并通过元件标号或网络名称加以说明。对于文档中涉及 MCU 存储器大小、内部时钟等仅能从芯片数据手册获取的信息，原理图中并没有明确标注，这类假设以 “❓ 未体现” 标注。

下面按照文档顺序逐条确认：

1. MCU 基本信息和配置
编号	假设	验证	实际情况说明 / 正确信息	相关位置
1.1	MCU 型号：WB32L003K8U6；封装：QFN32；参考标号：U6；芯片位置/方向标记	✅	原理图右侧 MCU 标注“U6 WB32L003K8U6(QFN32)”。引脚序号沿外框标记，方向角用小三角形表示。	U6 位于右侧区域 D/B。
1.2	Flash 64 KB (0x00000000–0x0000FFFF)；RAM 8 KB (0x20000000–0x20001FFF)；有无外部存储	❓	原理图只展示引脚及外部连接，没有标注存储容量，未见外部存储芯片。	原理图无此信息。
1.3	使用内部 HSI；系统 48 MHz；是否有外部晶振和外设分频	⚠️ 部分正确	原理图未标注时钟源，也未绘制外部晶振或晶体连接脚。WB32L003 标配内部 RC 振荡器。系统 48 MHz 需通过代码配置，不在原理图中体现。	无。
1.4	VDD 接 3.3 V；VSS 接 GND；有无外部复位电路；是否使用看门狗	✅ / ❓	VDD 与 3.3 V 网络相连，VSS 接地；NRST 引脚通过 10 K 上拉电阻和 100 nF 电容构成复位电路。看门狗为内部功能，原理图中未体现。	NRST 电路位于 MCU 左上角。
1.5	SWD 调试接口：SWDIO/SWDCLK；是否引出调试接口；复位引脚是否引出	❌	板子使用 STC U8‑Mini 烧录器接口（ISP_CK、ISP_D、ISP_NRST），没有 SWD 10-pin 标准口。ISP_NRST 经 10 K 上拉和 100 nF 电容，与 MCU NRST 相连。	烧录接口位于右上区域。

2. 引脚映射详细确认
2.1 SPI 接口引脚（存在多处定义冲突）
假设信号	代码定义	实际原理图连接	结果	说明
TFT_SPI_SCLK_PIN = PC5（SPI 时钟）	pin_config.h	PC5 在原理图用作 CON_POW_RF 控制 5 V 电源，不接 SPI。	❌	原理图没有将 PC5 用作 SPI 时钟。
TFT_SPI_MOSI_PIN = PC6	pin_config.h	PC6 用作 CON_POW_CPU，控制主电源。	❌	无 MOSI 连接。
TFT_CS_PIN = PC0	pin_config.h	TFT CS 实际在 PB4；PC0 用作 LED_RED。	❌	定义错误。
TFT_DC_PIN = PA3	pin_config.h	TFT 的数据/命令线 LCD_RS 实际接 PB6。	❌	不符。
TFT_RST_PIN = PD3	pin_config.h	TFT 复位 LCD_RESET 接 PB7。	❌	不符。
ST7735_CS/DC/RST=PB0/PB1/PB2	st7735.h	原理图中 PB0 为 SW_POW，PB1/2 用于背光控制。	❌	这组定义也与硬件不符。
结论		显示屏控制线全部在 PB7–PB0：PB7=LCD_RESET、PB6=LCD_RS、PB5=LCD_SCL、PB4=LCD_CS、PB3=LCD_SDA、PB2=CLD_BL2、PB1=CLD_BL1、PB0=SW_POW。SPI 接口并未使用 PC5/PC6/PC7。		

2.2 显示屏背光控制
假设	实际情况	结果
TFT_BL_PIN = PA1 使用 TIM2_CH2 PWM 驱动背光	背光控制由 PB1(CLD_BL1，背光高亮)和 PB2(CLD_BL2，背光低亮)组成；CLD_BL1 通过 Q7 控制背光正极电流，CLD_BL2 直接驱动背光阴极。PA1 未用于背光	❌
电流设置 0/5/12/20 mA	原理图未标注具体电流值，背光电流由串联电阻和 Q7 控制，约 22 Ω/220 Ω 设定。	❓

2.3 ADC 输入引脚
假设	实际情况	结果
PC4 = 左声道音频	正确	✅
PC3 = 右声道音频	正确	✅
PC0 = BAT_ADC	实际 BAT_ADC 为 PC2，经 1:2 分压	❌
PC0 同时定义为 TFT_CS、LED_RED	PC0 仅用于 LED_RED；TFT_CS 在 PB4	❌

2.4 按键输入
假设	实际情况	结果
KEY_PWR_PIN = PD6，按下接地	电源按键 SW_POW 直接与二极管 D302/D300 拉低 Q5 基极；同时引脚 PB0 (SW_POW) 连接 MCU 检测按键状态。MCU PD6 不连接按键，而接收充满检测 CHAR	❌
KEY_MODE_PIN = PD4，按下接地	未有模式按键，立体声/单声道由外部滑动开关和 MCU 输出 CON_STEREO 控制。PD4 未使用。	❌

2.5 LED 指示灯
假设	实际情况	结果
LED_RED_PIN = PC0；LED_GREEN_PIN = PC1	正确：PC0 接 LED_RED，PC1 接 LED_GREEN，共阳驱动	✅
第二组 GREEN_PIN=PB3/RED_PIN=PB4	未使用；PB3/4 分别用于 LCD_SDA 和 LCD_CS。	❌
LED 是共阳还是共阴	共阳，MCU 下拉点亮	✅

2.6 音频控制引脚
假设	实际情况	结果
MUTE_PIN = PA4，高=解除静音	原理图标注 “CON_MUTE(低电平静音)” 连接 PA4，高电平正常工作	✅（说明相反）
V2_PIN = PB6 (STEREO/MONO)	立体声控制线为 CON_STEREO，连接 MCU PD3，低电平立体声，高电平单声道。PB6 为 LCD_RS，不用于模式选择	❌
MODE_OUT_PIN = PB2	未见 MODE_OUT；PB2 为 CLD_BL2(背光低亮)	❌

2.7 电源管理引脚（pin_config.h 中已注释）
CON_POW_PIN (PB7) 和 CON_LCD_PIN (PB6) 在代码中被注释。这两个名称对应原理图中的 SW_POW（PB0）和 CON_POW_LCD（PA3）。原理图中确实存在电源控制信号：

CON_POW_CPU：PC6 控制主电源 Q5；

CON_POW_RF：PC5 控制 5 V 电源 Q4/Q2；

CON_POW_LCD：PA3 控制 LCD 电源。
因此 pin_config.h 中注释的引脚与实际信号不符；MCU 控制电源开关是真实存在的。

2.8 充电检测
充电状态检测并不在 PB5。TP4056 的 CHRG# 引脚没有连接 MCU。
真正用于监测充电的是：

DET：充电 5 V 检测，经 R3/R4 分压送 PD7；

CHAR：充满检测，经 R15 送 PD6；

PB5 用于其他用途（未连接），故代码中的 PB5 定义不正确。

2.9 UART 调试接口
代码中把 PB6/PD6 复用为调试 UART，但原理图没有为 UART 引出连接器。

PB6 实际为 LCD_RS；

PD6 为充满检测信号 CHAR；

板子使用 ISP_CK/ISP_D/ISP_NRST 接口烧录，UART 调试未实现。
因此 UART 调试功能和 KEY/PWR 引脚存在冲突，硬件中没有使用。

2.10 其他未明确用途的引脚
PA2：在电源部分连接 3.3 V 去耦电容和 CON_POW_LCD，未单独作为 GPIO；代码中将其初始化为输出但未使用。

PD0 和 PD4：未连接任何网络，留空。

PB5：未连接。

3. 外设配置和连接
SPI1 外设：原理图没有标出 SPI1 的具体使用，因液晶通过 PB7..PB3 的模拟 SPI 接口驱动，PC5/PC6 不启用 SPI 功能。

ADC 配置：左、右通道分别通过 RC4580 运放滤波后送入 PC4/PC3；电池电压经分压送 PC2。采样率、分辨率和 DMA 设置在代码中实现，原理图无相关信息。

TIM2/TIM3 配置：原理图只体现 PA3/PA4/PA1 等引脚，未标注具体定时器功能。

4. 显示屏接口详细规格
型号/驱动芯片：连接器标注 “LH096NT”，代码使用 ST7735 驱动，推测屏幕可能兼容 ST7735 系列。

接口类型：八条线构成 4 线 SPI 接口（RESET、RS、SDA、SCL、CS）加两条背光控制、1 条电源、1 条 GND。

初始化时序：原理图中未给出复位延时等时序参数，需参考屏幕数据手册。

5. 音频信号路径
音频输入：由连接器 J6 的 L_AF/R_AF 输入，经 RC4580 运放和二极管限幅后送入 CD4066 模拟开关。

运放电路：板上有 U1A/U1B 和 U10A/U10B 共两颗 RC4580 双运放，以及 U3 为 MAX97220 耳机放大器。

ADC 输入：运放输出经模拟开关切换后接 PC3/PC4 (AF_R/AF_L)。输入钳位和分压在原理图中已实现。

6. 电源管理架构
电源来源：USB Type‑C 5 V 经过二极管 D1 给 TP4056 充电芯片；电池经 TP4056 供电。

3.3 V 产生：电池经 Q5（6402 PNP 管）后送到 DC‑DC 芯片 HX4002B (U2)，产生稳压 3.3 V。

5 V 产生：Q4/Q2 控制电池供电到 HX4004 (U5) 产生 5 V，为 MAX97220 运放供电。

电源控制信号：CON_POW_CPU (PC6) 控制 Q5；CON_POW_RF (PC5) 控制 Q4/Q2；CON_POW_LCD (PA3) 通过 R22/C45/C46 向 LCD 供电。

Power Good 信号：原理图没有专用 PGOOD 输出，MCU 通过 ADC 检测 V_Batt 或检测充电 5 V。

7. 电池管理
电池管理芯片：U7 为 TP4056，提供恒流/恒压充电控制。

充电检测：DET (PD7) 检测 USB 电源；CHAR (PD6) 检测充满状态；CHRG# 未连接 MCU。

电量检测：PC2 通过 R3/R4/R5 分压测量电池电压（1:2 比例）。低电压阈值未在原理图中给出，由固件定义。

8. 信号完整性和电气特性
数字信号：背光控制等均有上拉/下拉电阻，如 R75 (22 K)、R63 (4.7 K)。

模拟信号：输入通过 RC 网络、RC4580 运放和二极管 BAT54S 提供过压保护。

电源去耦：每个电源 IC 输入输出都配有 100 nF、10 µF 等去耦电容。

9. PCB 布局、机械和连接器等
连接器 J5/J6：两个 8 针连接器提供电源、音频输入输出和立体声控制。

调试接口：使用 5 针烧录座 (ISP_CK/ISP_D/+3.3 V/NRST/GND) 连接 STC U8‑Mini。没有 SWD 接口。

按键：仅有一个上电按钮 SW_POW；另一个立体声开关为滑动开关，接外部板。

其他：原理图未标注外壳、PCB 尺寸等机械信息。

10. 代码假设与原理图差异汇总
SPI 引脚冲突：代码使用 PC5/6 作为 SPI，硬件用 PB7..PB4。需修改代码。

LED 定义：代码存在两组 LED 引脚，实际应使用 PC0/PC1。

背光控制：代码定义 PA1 为背光，但硬件使用 PB1/PB2 + Q7 控制。

按键处理：KEY_PWR/KEY_MODE 引脚分配不正确。SW_POW 在 PB0；PD6/PD4 未用于按键。

模式输出/立体声控制：代码中的 MODE_OUT 与 V2_PIN 均不符合硬件。应使用 PD3 输出 CON_STEREO。

电池检测：电池电压来自 PC2，而非 PC0。分压比为 1:2。

调试接口：代码配置 UART 调试，但硬件未引出；应改用 ISP 接口烧录。

此外，原理图中还有部分功能在代码中未体现，例如音频滤波运放、电源控制信号、充电检测 DET/CHAR 等，建议固件适当读取 PD6/PD7 状态、控制 PC5/PC6/PA3/PA4/PB1/PB2 等，实现电源管理和背光调节。

结论
通过对原理图逐条核对，发现代码中的引脚配置、按键和电源控制等多处与硬件不符。建议您根据上述表格修正 pin_config.h、st7735.h 以及硬件控制逻辑，特别是：

更新 TFT 引脚到 PB7–PB4/PB3/PB2/PB1/PB0。

将电池检测引脚改为 PC2。

使用 PD3 控制立体声/单声道，PC5/PC6 控制电源。

调整背光 PWM 到 PB1/PB2 控制 Q7。

使用 PD6/PD7 检测充满状态和插入电源。

如果后续还有其他硬件方面的问题，欢迎继续交流。